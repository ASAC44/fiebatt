services:
  frontend:
    build:
      context: ..
      dockerfile: infra/Dockerfile.frontend
      args:
        NEXT_PUBLIC_BACKEND_URL: ${NEXT_PUBLIC_BACKEND_URL:-http://backend:8000}
    env_file:
      - ../.env
    environment:
      NEXT_PUBLIC_BACKEND_URL: ${NEXT_PUBLIC_BACKEND_URL:-http://backend:8000}
    ports:
      - "3001:3001"
    depends_on:
      - backend

  backend:
    build:
      context: ..
      dockerfile: infra/Dockerfile.backend
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://fiebatt:fiebatt@db:5432/fiebatt}
      GPU_WORKER_URL: ${GPU_WORKER_URL:-http://gpu-worker:8001}
      USE_AI_STUBS: ${USE_AI_STUBS:-true}
    volumes:
      - storage:/app/storage
    depends_on:
      - db

  gpu-worker:
    profiles: ["gpu"]
    build:
      context: ..
      dockerfile: infra/Dockerfile.gpu
    ports:
      - "8001:8001"
    volumes:
      - ../apps/gpu-worker/checkpoints:/app/checkpoints:ro

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fiebatt
      POSTGRES_PASSWORD: fiebatt
      POSTGRES_DB: fiebatt
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  storage:
  pgdata:
