{
  "name": "fiebatt-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/frontend run dev",
    "build": "npm --prefix apps/frontend run build",
    "lint": "npm --prefix apps/frontend run lint",
    "web:dev": "npm --prefix apps/frontend run dev",
    "web:build": "npm --prefix apps/frontend run build",
    "web:start": "npm --prefix apps/frontend run start",
    "web:lint": "npm --prefix apps/frontend run lint"
  }
}
